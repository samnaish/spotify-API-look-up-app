version: '3'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./dist/:/usr/share/nginx/html/
    restart: unless-stopped

  mongo:
    image: 'mongo:3.4.1'
    ports:
      - '27017:27017'
    volumes:
      - ./db:/var/lib/
    restart: unless-stopped
